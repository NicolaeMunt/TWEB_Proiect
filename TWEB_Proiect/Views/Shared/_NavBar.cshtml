@{
    ViewBag.Title = "_NavBar";
}

<!--======== Top Bar =======-->
<div class="top-bar">
    <div class="container">
        <div class="row">
            <div class="col-md-6">
                <div class="navbar-menu-left-side239">
                    <ul>
                        <li><a href="@Url.Action("Contact", "Home")"><i class="fa fa-envelope-o" aria-hidden="true"></i>Contact</a></li>
                        <li><a href="#"><i class="fa fa-headphones" aria-hidden="true"></i>Ajutor</a></li>
                        @if (Session["UserId"] != null)
                        {
                            <li><a href="@Url.Action("Index", "Home")"><i class="fa fa-user" aria-hidden="true"></i>Salut, @Session["Username"]</a></li>
                        }
                        else
                        {
                            <li><a href="@Url.Action("Login", "Account")"><i class="fa fa-user" aria-hidden="true"></i>Login</a></li>
                        }
                    </ul>
                </div>
            </div>
            <div class="col-md-6">
                <div class="navbar-serch-right-side">
                    <form class="navbar-form" role="search">
                        <div class="input-group add-on">
                            <input class="form-control form-control222" placeholder="Search" id="srch-term" type="text">
                            <div class="input-group-btn">
                                <button class="btn btn-default btn-default2913" type="button"><i class="glyphicon glyphicon-search"></i></button>
                            </div>
                        </div>
                    </form>
                </div>
            </div>
        </div>
    </div>
</div>

<!-- ==========Header Mega Navbar=======-->
<div class="top-menu-bottom932">
    <nav class="navbar navbar-default">
        <div class="container">
            <!-- Brand and toggle get grouped for better mobile display -->
            <div class="navbar-header">
                <button type="button" class="navbar-toggle collapsed" data-toggle="collapse" data-target="#bs-example-navbar-collapse-1" aria-expanded="false">
                    <span class="sr-only">Toggle navigation</span>
                    <span class="icon-bar"></span>
                    <span class="icon-bar"></span>
                    <span class="icon-bar"></span>
                </button>
                <a class="navbar-brand" href="@Url.Action("Index", "Home")">
                    <img src="~/Content/image/logo.png" alt="INJÎNER Logo">
                </a>
            </div>

            <!-- Collect the nav links, forms, and other content for toggling -->
            <div class="collapse navbar-collapse" id="bs-example-navbar-collapse-1">
                <ul class="nav navbar-nav navbar-right">
                    <!-- Основные страницы -->
                    <li><a href="@Url.Action("About", "Home")">Despre</a></li>
                    <li><a href="@Url.Action("Ask", "Question")">Ask Question</a></li>

                    <!-- Question Dropdown -->
                    <li class="dropdown">
                        <a href="#" class="dropdown-toggle" data-toggle="dropdown" role="button" aria-haspopup="true" aria-expanded="false">Question <span class="caret"></span></a>
                        <ul class="dropdown-menu animated zoomIn">
                            <li><a href="@Url.Action("List", "Question")">All Questions</a></li>
                            <li><a href="@Url.Action("Ask", "Question")">Ask Question</a></li>
                            <li role="separator" class="divider"></li>
                            <li><a href="#">HTML</a></li>
                            <li><a href="#">CSS</a></li>
                            <li><a href="#">Javascript</a></li>
                            <li><a href="#">Bootstrap</a></li>
                        </ul>
                    </li>

                    <!-- Blog Dropdown -->
                    <li class="dropdown">
                        <a href="#" class="dropdown-toggle" data-toggle="dropdown" role="button" aria-haspopup="true" aria-expanded="false">Blog <span class="caret"></span></a>
                        <ul class="dropdown-menu animated zoomIn">
                            <li><a href="@Url.Action("Blog", "Home")">Blog</a></li>
                        </ul>
                    </li>

                    <!-- Page Dropdown -->
                    <li class="dropdown">
                        <a href="#" class="dropdown-toggle" data-toggle="dropdown" role="button" aria-haspopup="true" aria-expanded="false">Page <span class="caret"></span></a>
                        <ul class="dropdown-menu animated zoomIn">
                            @if (Session["UserId"] == null)
                            {
                                <li><a href="@Url.Action("Login", "Account")">Login</a></li>
                                <li><a href="@Url.Action("Register", "Account")">Register</a></li>
                            }
                            else
                            {
                                <li><a href="@Url.Action("Index", "Home")">Profile</a></li>
                                <li><a href="@Url.Action("Logout", "Account")">Logout</a></li>
                            }
                            <li role="separator" class="divider"></li>
                            <li><a href="@Url.Action("Contact", "Home")">Contact</a></li>
                            <li><a href="@Url.Action("Ask", "Question")">Întrebări</a></li>
                            <li><a href="#">Utilizatori</a></li>
                            <li><a href="#">Categorii</a></li>
                        </ul>
                    </li>

                    <!-- Contact -->
                    <li><a href="@Url.Action("Contact", "Home")">Contact us</a></li>

                    <!-- Auth Links (Right Side) -->
                    @if (Session["UserId"] != null)
                    {
                        <li class="dropdown">
                            <a href="#" class="dropdown-toggle" data-toggle="dropdown" role="button" aria-haspopup="true" aria-expanded="false">
                                <i class="fa fa-user"></i> @Session["Username"] <span class="caret"></span>
                            </a>
                            <ul class="dropdown-menu">
                                <li><a href="@Url.Action("Index", "Home")"><i class="fa fa-user"></i> Profile</a></li>
                                <li><a href="#"><i class="fa fa-cog"></i> Settings</a></li>
                                <li role="separator" class="divider"></li>
                                <li><a href="@Url.Action("Logout", "Account")"><i class="fa fa-sign-out"></i> Ieșire</a></li>
                            </ul>
                        </li>
                    }
                    else
                    {
                        <li><a href="@Url.Action("Register", "Account")"><i class="fa fa-user-plus"></i> Registrare</a></li>
                        <li><a href="@Url.Action("Login", "Account")"><i class="fa fa-sign-in"></i> Intrare</a></li>
                    }
                </ul>
            </div>
            <!-- /.navbar-collapse -->
        </div>
        <!-- /.container-fluid -->
    </nav>
</div>